STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-v2

# Transactional email (order confirmation / shipped notices).
# Leave empty to log emails to the console instead of sending.
# Zero-cost option: a Gmail address + app password (myaccount.google.com/apppasswords)
#   SMTP_HOST=smtp.gmail.com  SMTP_PORT=465  SMTP_USER=you@gmail.com  SMTP_PASS=<app password>
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Protein Pasal <orders@proteinpasal.com>"
# Public storefront origin used in email links (tracking deep links).
STOREFRONT_URL=https://gym-ecommerce-six.vercel.app
